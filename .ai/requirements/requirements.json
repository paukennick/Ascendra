{
  "version": "1.0.0",
  "requirement_id_prefix": "REQ",
  "requirements": [
    {
      "id": "REQ-001",
      "category": "AI workspace ruleset",
      "title": "Add universal engineering ruleset",
      "description": "Create the controlling OmniContext engineering ruleset and align the workspace instructions to it.",
      "priority": "high",
      "status": "completed",
      "minimum_access_scope": [
        ".ai/core-context.md",
        ".ai/rules/universal-engineering-ruleset.json",
        ".ai/rules/controlled-implementation.md",
        ".ai/rules/completion-workflow.md",
        ".ai/project-configuration.md",
        "make_ai.py",
        "CLAUDE.md",
        ".cursorrules",
        ".github/copilot-instructions.md",
        "CHANGELOG.md"
      ],
      "acceptance_criteria": [
        "The global ruleset exists as a framework-agnostic source of truth.",
        "Assistant entrypoint shims point to the .ai entrypoint sources.",
        "Completion workflow requires validation, documentation, changelog, and reporting."
      ],
      "validation_required": [
        "json",
        "python_compile",
        "bootstrap"
      ],
      "documentation_required": [
        "changelog",
        "readme"
      ],
      "risk_notes": [
        "Project-specific placeholders must be filled before use in a target repository."
      ]
    },
    {
      "id": "REQ-002",
      "category": "Documentation",
      "title": "Create OmniContext README and visual",
      "description": "Build the public-facing README around OmniContext and include an architecture diagram.",
      "priority": "medium",
      "status": "completed",
      "minimum_access_scope": [
        "README.md",
        "assets/omni-context.svg",
        "CHANGELOG.md"
      ],
      "acceptance_criteria": [
        "README explains the OmniContext problem, pattern, setup, daily use, and validation workflow.",
        "README includes an SVG architecture diagram.",
        "Changelog records the documentation update."
      ],
      "validation_required": [
        "svg_xml_parse",
        "json",
        "python_compile",
        "bootstrap"
      ],
      "documentation_required": [
        "readme",
        "changelog"
      ],
      "risk_notes": [
        "License text remains project-specific and should be filled before publication."
      ]
    },
    {
      "id": "REQ-003",
      "category": "Executable validation",
      "title": "Add OmniContext doctor and requirement registry",
      "description": "Make the workspace more powerful by adding a dependency-free CLI doctor, requirement registry, and schema-backed validation documents.",
      "priority": "high",
      "status": "completed",
      "minimum_access_scope": [
        "make_ai.py",
        ".ai/requirements/requirements.json",
        ".ai/schemas/requirements.schema.json",
        ".ai/schemas/universal-engineering-ruleset.schema.json",
        "README.md",
        "CHANGELOG.md"
      ],
      "acceptance_criteria": [
        "CLI supports sync, doctor, and validate commands.",
        "Doctor validates required files, JSON parsing, ruleset keys, requirements structure, assistant pointer drift, README architecture reference, and changelog presence.",
        "Requirements registry tracks completed project requirements.",
        "README documents the new CLI workflow."
      ],
      "validation_required": [
        "doctor",
        "json",
        "python_compile",
        "bootstrap"
      ],
      "documentation_required": [
        "readme",
        "changelog"
      ],
      "risk_notes": [
        "Doctor currently implements lightweight schema checks with the Python standard library rather than a full JSON Schema engine."
      ]
    },
    {
      "id": "REQ-004",
      "category": "Assistant fallback rules",
      "title": "Centralize fallback rules for LLM entrypoints",
      "description": "Add explicit fallback operating rules for assistant entrypoints in case an assistant skips or cannot follow the primary .ai configuration.",
      "priority": "high",
      "status": "completed",
      "minimum_access_scope": [
        ".ai/entrypoints/fallback-contract.md",
        ".ai/rules/fallback-llm-rules.json",
        ".ai/entrypoints/",
        "CLAUDE.md",
        ".cursorrules",
        ".github/copilot-instructions.md",
        "make_ai.py",
        ".ai/requirements/requirements.json",
        "README.md",
        "CHANGELOG.md"
      ],
      "acceptance_criteria": [
        "A shared fallback contract exists inside .ai/entrypoints.",
        "A structured fallback rulepack exists inside .ai/rules.",
        "All assistant entrypoint shims point to .ai entrypoint sources and the centralized fallback contract.",
        "The sync command regenerates .ai entrypoint sources and assistant shims.",
        "The sync command skips existing non-Omni files unless --force is used.",
        "Doctor validates the fallback contract and fallback rulepack as required source-of-truth files.",
        "README and changelog document the fallback behavior."
      ],
      "validation_required": [
        "doctor",
        "validate",
        "sync",
        "json",
        "python_compile"
      ],
      "documentation_required": [
        "readme",
        "changelog"
      ],
      "risk_notes": [
        "Fallback rules intentionally duplicate core constraints in a centralized fallback contract to reduce skipped-context risk without bloating root shims."
      ]
    },
    {
      "id": "REQ-005",
      "category": "Documentation clarity",
      "title": "Clarify OmniContext purpose and make_ai role",
      "description": "Update README positioning to explain the unique context-drift use case and clarify that make_ai.py is optional maintenance tooling, not required for normal cloned-repo usage.",
      "priority": "high",
      "status": "completed",
      "minimum_access_scope": [
        "README.md",
        "make_ai.py",
        ".ai/requirements/requirements.json",
        "CHANGELOG.md"
      ],
      "acceptance_criteria": [
        "README explains OmniContext as a repo-local control plane for AI coding assistants.",
        "README explicitly states the unique problem is AI context drift across tools.",
        "README explains that cloned repositories do not require make_ai.py for normal usage.",
        "make_ai.py help text describes the script as maintenance tooling.",
        "Changelog records the documentation clarification."
      ],
      "validation_required": [
        "doctor",
        "validate",
        "json",
        "python_compile"
      ],
      "documentation_required": [
        "readme",
        "changelog"
      ],
      "risk_notes": [
        "Clarification avoids presenting the maintenance script as mandatory setup for users who clone a complete repository."
      ]
    },
    {
      "id": "REQ-006",
      "category": "Ruleset refinement",
      "title": "Replace Python data rules with data governance rules",
      "description": "Rename the Python-specific data rule file and refocus it on bounded, framework-agnostic data governance.",
      "priority": "medium",
      "status": "completed",
      "minimum_access_scope": [
        ".ai/rules/python-data.md",
        ".ai/rules/data-governance.md",
        "make_ai.py",
        "README.md",
        ".ai/requirements/requirements.json",
        "CHANGELOG.md"
      ],
      "acceptance_criteria": [
        "Python-specific data rules are removed from the universal ruleset surface.",
        "A data-governance rule file covers data boundaries, contracts, validation, transformations, privacy, persistence, migrations, and testing.",
        "Doctor validates the renamed rule file as required.",
        "README references the new data governance rule name.",
        "Changelog records the ruleset refinement."
      ],
      "validation_required": [
        "doctor",
        "validate",
        "json",
        "python_compile"
      ],
      "documentation_required": [
        "readme",
        "changelog"
      ],
      "risk_notes": [
        "The rule is intentionally bounded to data behavior and avoids language-specific implementation guidance."
      ]
    },
    {
      "id": "REQ-007",
      "category": "Structured rulepacks",
      "title": "Convert enforceable rule files to JSON",
      "description": "Replace Markdown rule implementations with schema-backed JSON rulepacks so doctor can validate rule IDs, severities, scopes, and statements.",
      "priority": "high",
      "status": "completed",
      "minimum_access_scope": [
        ".ai/rules/controlled-implementation.md",
        ".ai/rules/controlled-implementation.json",
        ".ai/rules/completion-workflow.md",
        ".ai/rules/completion-workflow.json",
        ".ai/rules/data-governance.md",
        ".ai/rules/data-governance.json",
        ".ai/rules/fallback-llm-rules.md",
        ".ai/rules/fallback-llm-rules.json",
        ".ai/rules/hci-ui-rules.md",
        ".ai/rules/hci-ui-rules.json",
        ".ai/rules/oop-design.md",
        ".ai/rules/oop-design.json",
        ".ai/schemas/rulepack.schema.json",
        "make_ai.py",
        "README.md",
        ".ai/requirements/requirements.json",
        "CHANGELOG.md"
      ],
      "acceptance_criteria": [
        "Enforceable companion rule files are represented as JSON rulepacks.",
        "Old Markdown rule implementation files are removed from .ai/rules.",
        "Each JSON rulepack contains stable rule IDs, severities, scopes, and statements.",
        "A rulepack schema documents the structured rule format.",
        "Doctor validates structured rulepacks.",
        "README documents JSON rulepacks instead of Markdown rule implementations."
      ],
      "validation_required": [
        "doctor",
        "validate",
        "sync",
        "json",
        "python_compile"
      ],
      "documentation_required": [
        "readme",
        "changelog"
      ],
      "risk_notes": [
        "Markdown remains appropriate for README and core human-facing overview files; enforceable policy is now JSON."
      ]
    },
    {
      "id": "REQ-008",
      "category": "Authoring interface",
      "title": "Add low-friction rule and requirement editing",
      "description": "Create an easy CLI interface for adding requirements and rulepack rules without manually editing long JSON files.",
      "priority": "high",
      "status": "completed",
      "minimum_access_scope": [
        "make_ai.py",
        "README.md",
        ".ai/requirements/requirements.json",
        "CHANGELOG.md"
      ],
      "acceptance_criteria": [
        "CLI can add requirements with defaults",
        "CLI can add rules to rulepacks with defaults",
        "README documents the authoring interface",
        "Doctor validates the resulting JSON"
      ],
      "validation_required": [
        "doctor",
        "validate",
        "json",
        "python_compile"
      ],
      "documentation_required": [
        "readme",
        "changelog"
      ],
      "risk_notes": [
        "CLI intentionally stays lightweight and flag-based to avoid adding dependencies"
      ]
    },
    {
      "id": "REQ-009",
      "category": "CLI ergonomics",
      "title": "Add omni command entrypoint",
      "description": "Provide an easy omni command so users can run omni doctor and omni sync instead of python3 make_ai.py doctor and python3 make_ai.py sync.",
      "priority": "high",
      "status": "completed",
      "minimum_access_scope": [
        "omni",
        "pyproject.toml",
        "make_ai.py",
        "README.md",
        ".ai/requirements/requirements.json",
        "CHANGELOG.md",
        "CLAUDE.md",
        ".cursorrules",
        ".github/copilot-instructions.md"
      ],
      "acceptance_criteria": [
        "Repo-local ./omni command works",
        "Editable install can expose an optional omni console script",
        "Doctor validates the repo-local command entrypoint and warns, rather than fails, when the optional console script is absent",
        "README documents omni doctor and omni sync",
        "Fallback instructions use omni commands"
      ],
      "validation_required": [
        "doctor",
        "validate",
        "sync",
        "json",
        "python_compile",
        "omni_doctor",
        "omni_sync"
      ],
      "documentation_required": [
        "readme",
        "changelog"
      ],
      "risk_notes": [
        "Exact omni command without ./ requires installing the package or adding the repo root to PATH; existing projects do not need to modify pyproject.toml to use ./omni"
      ]
    },
    {
      "id": "REQ-010",
      "category": "Compatibility",
      "title": "Remove tomllib dependency",
      "description": "Remove the Python 3.11-only tomllib import so editors using Python 3.10 do not report unresolved imports.",
      "priority": "medium",
      "status": "completed",
      "minimum_access_scope": [
        "make_ai.py",
        "pyproject.toml",
        "README.md",
        ".ai/requirements/requirements.json",
        "CHANGELOG.md"
      ],
      "acceptance_criteria": [
        "make_ai.py has no tomllib import",
        "Doctor treats the pyproject console script as optional",
        "Package metadata supports Python 3.10 or newer",
        "Validation passes"
      ],
      "validation_required": [
        "doctor",
        "validate",
        "json",
        "python_compile"
      ],
      "documentation_required": [
        "readme",
        "changelog"
      ],
      "risk_notes": [
        "pyproject validation is intentionally lightweight and checks the expected console script line rather than fully parsing TOML"
      ]
    },
    {
      "id": "REQ-011",
      "category": "Doctor / drift detection",
      "title": "Add project-map freshness and changelog-drift checks to omni doctor",
      "description": "Two generic omni doctor checks, developed while auditing an adopter project (STEP) for OmniEngineering alignment and ported back here so every adopter benefits: validate_project_map_freshness warns when a top-level directory that exists on disk is missing from .ai/project-map.md's tree (catches a map that was never regenerated after a structural change); validate_recent_commits_tracked warns when commits postdate -- by commit timestamp, not git log X..HEAD DAG reachability, which squash-merge PR histories make misleading -- the last commit that touched CHANGELOG.md, naming each one so untracked work is caught automatically instead of by manual audit.",
      "priority": "high",
      "status": "completed",
      "minimum_access_scope": [
        "make_ai.py"
      ],
      "acceptance_criteria": [
        "omni doctor warns when an ALLOWED_ROOT_DIRS directory present on disk is absent from the generated project map's directory tree.",
        "omni doctor warns, listing each commit, when commits postdate (by commit date) the last commit that touched CHANGELOG.md.",
        "Both checks are no-ops (skip silently) when git or the relevant files are unavailable, so they never turn into false errors on a non-git or freshly-adopted workspace."
      ],
      "validation_required": [
        "manual_verification"
      ],
      "documentation_required": [
        "changelog"
      ],
      "risk_notes": [
        "The commit-date comparison intentionally avoids `git log <hash>..HEAD` because squash-merge PR workflows make already-integrated side-branch commits appear \"after\" the changelog commit in DAG-reachability terms even though they landed chronologically earlier; using commit timestamps instead avoids that false positive."
      ]
    },
    {
      "id": "REQ-012",
      "category": "CI / repo hygiene",
      "title": "Wire omni doctor into CI and stop future line-ending drift",
      "description": "Two efficiency/maintainability fixes identified while discussing how to make OmniEngineering easier to integrate and keep aligned over time: (1) added .github/workflows/ci.yml running `python3 omni doctor` (full-history checkout) on every push/PR to main, so workspace-health drift is caught automatically instead of relying on someone manually running the command or an LLM session auditing on request; (2) added .gitattributes (`* text=auto eol=lf`) so a Windows/WSL checkout stops silently diverging from committed line endings -- this repo's entire working tree had drifted to CRLF against an LF-committed history, which was masking real diffs behind whole-file noise.",
      "priority": "medium",
      "status": "completed",
      "minimum_access_scope": [
        ".github/workflows/ci.yml",
        ".gitattributes"
      ],
      "acceptance_criteria": [
        "omni doctor runs automatically on every push and pull request to main and fails the build only on real errors, not warnings.",
        ".gitattributes normalizes text files to LF so future checkouts don't silently drift from committed line endings.",
        "Existing committed content is left untouched -- .gitattributes affects future checkouts/commits, not a retroactive rewrite."
      ],
      "validation_required": [
        "manual_verification"
      ],
      "documentation_required": [
        "changelog"
      ],
      "risk_notes": [
        "This repo's existing tracked files are still CRLF in the working tree as of this change; .gitattributes stops new drift but does not retroactively renormalize what's already committed. A follow-up `git add --renormalize .` commit would fix that but was intentionally left out of scope here since it would touch every file in the repo."
      ]
    },
    {
      "id": "REQ-013",
      "category": "CLI / template maintainability",
      "title": "Add omni update: 3-way-merge template improvements into adopted workspaces",
      "description": "The single biggest 'clunky' gap raised this session: once a project adopts OmniEngineering and customizes its rules/playbooks/checklists, there was no way to pull later template improvements in except a manual hand-diff-and-port (done twice by hand earlier this session). Adds .ai/omni-version.json (written by omni adopt, recording the source path and git ref at last sync) and a new 'omni update --source <checkout>' command that 3-way-merges every template-managed file via git merge-file: files the adopter never touched are fast-path overwritten, files only the template changed are left alone, files both sides changed are merged (or left with conflict markers on a genuine collision), and .ai/project-configuration.md, .ai/project-map.md, .ai/requirements/requirements.json, and CHANGELOG.md are never touched since those are adopter-owned. If make_ai.py itself changes, update automatically re-runs sync in a fresh process so entrypoint/pointer/ignore files regenerate from the new templates. omni doctor gained a matching check warning when .ai/omni-version.json is missing. Also adds `--bootstrap` to `omni update` (records --source's current ref as the starting point without merging anything, for a workspace adopted before update-tracking existed) and a README Quick Start section covering the adopt/doctor/update loop end to end, including the bootstrap path, since a working feature nobody can find or follow doesn't solve the 'clunky to integrate' complaint either.",
      "priority": "high",
      "status": "completed",
      "minimum_access_scope": [
        "make_ai.py",
        "README.md"
      ],
      "acceptance_criteria": [
        "omni adopt writes .ai/omni-version.json on success.",
        "omni update fast-path-overwrites adopter-untouched files, no-ops on adopter-only changes, cleanly merges non-overlapping changes on both sides, and leaves conflict markers on overlapping changes -- verified end to end in a scratch adopter workspace covering all four cases plus confirming requirements.json/project-map.md/project-configuration.md/CHANGELOG.md are never touched.",
        "omni update --bootstrap writes .ai/omni-version.json against --source's current ref and merges nothing, verified end to end.",
        "omni doctor warns when .ai/omni-version.json is missing.",
        "README Quick Start section documents adopt, doctor, update, and the bootstrap path for pre-existing adopters."
      ],
      "validation_required": [
        "manual_verification"
      ],
      "documentation_required": [
        "changelog",
        "readme"
      ],
      "risk_notes": [
        "Requires --source to be a real git checkout (needed to reconstruct the 3-way-merge base via git show); a plain folder copy as source is explicitly rejected with a clear message rather than guessing.",
        "Fetching a template source directly from a remote git URL is an intentional follow-up, not included here -- v1 matches omni adopt's existing local-checkout-path convention."
      ]
    },
    {
      "id": "REQ-014",
      "category": "CLI / template maintainability",
      "title": "Fix: omni update must not re-run sync when make_ai.py itself has an unresolved conflict",
      "description": "Found live while dogfooding REQ-013 against STEP_App's genuinely diverged make_ai.py: when make_ai.py's own merge produced a conflict, the file was written with unresolved <<<<<<< markers (correct), but run_update still treated that as make_ai.py having 'changed' and re-ran sync in a fresh subprocess against the now-syntactically-invalid file, producing a raw Python SyntaxError traceback instead of a clean message. Excluded the conflict outcome from the make_ai_changed re-sync trigger, and added an explicit message telling the user sync was skipped and why, and to run it themselves once the conflict is resolved.",
      "priority": "medium",
      "status": "completed",
      "minimum_access_scope": [
        "make_ai.py"
      ],
      "acceptance_criteria": [
        "omni update no longer attempts to re-run sync when make_ai.py itself has an unresolved conflict; prints a clear explanatory message instead of a raw traceback. Verified by reproducing the original crash against STEP_App before the fix and confirming a clean message after."
      ],
      "validation_required": [
        "manual_verification"
      ],
      "documentation_required": [
        "changelog"
      ],
      "risk_notes": []
    },
    {
      "id": "REQ-015",
      "category": "Code understanding",
      "title": "Add omni graph: a deterministic tree-sitter AST code graph, not a vector index",
      "description": "Adds `omni graph build/trace/show`, a new omni_graph.py module holding a local, deterministic code graph parsed from source with tree-sitter -- no embeddings, no vector store. Every node is a code entity read straight from a syntax tree (module/class/function/method); every edge is tagged EXTRACTED (a fact read directly from a single source site: an import statement, a call site, a base class) or INFERRED (resolved by traversing the graph across files/scopes, or by an optional configured semantic API pass). `omni graph trace <a> <b>` answers 'how are these connected' with an actual shortest path of tagged hops instead of a similarity score; `omni graph show <node>` lists a symbol's direct EXTRACTED/INFERRED edges. Covers Python, JavaScript, and TypeScript in v1. tree-sitter and the per-language grammars are an optional `[graph]` extra so the workspace stays dependency-free by default: `omni graph build` fails with an actionable install message if the extra is missing, while `trace`/`show` only read the JSON `build` wrote and need nothing beyond the standard library. The semantic pass is opt-in via `--semantic` and only calls out to a network API if OMNI_GRAPH_SEMANTIC_API_URL is explicitly configured; its findings are still validated against known graph symbol names before being added as INFERRED edges, so a hallucinated relation can't be written into the graph silently. The CLI surface (subcommand-per-verb, a --json mode on every command) is intentionally 1:1 wrappable by an MCP relay the same way omni_map/omni_doctor/omni_sync already are.",
      "priority": "high",
      "status": "completed",
      "minimum_access_scope": [
        "omni_graph.py",
        "make_ai.py",
        "pyproject.toml",
        "README.md"
      ],
      "acceptance_criteria": [
        "omni graph build parses Python, JavaScript, and TypeScript source with tree-sitter and writes a JSON graph whose edges are each tagged provenance EXTRACTED or INFERRED, with a resolver field distinguishing syntax / graph-traversal / semantic-api.",
        "omni graph build fails with a clear install message (not a traceback) when tree-sitter or a language grammar is missing, verified by running it against the system Python with no [graph] extra installed.",
        "omni graph trace <a> <b> resolves symbol names (preferring real symbols over unresolved external placeholders on a name collision) and prints the shortest path between them as a sequence of tagged hops; ambiguous or missing names are reported, not guessed.",
        "omni graph show <node> lists a symbol's direct outgoing and incoming edges with provenance tags.",
        "Cross-file call/import/inheritance resolution (INFERRED edges) verified against this repository's own make_ai.py plus a scratch TypeScript fixture with a cross-file import, confirming resolved edges are correct and that common built-ins (e.g. print) are excluded from the ambiguous unique-name fallback.",
        "omni graph build/trace/show and the graph JSON never require network access unless --semantic is passed and OMNI_GRAPH_SEMANTIC_API_URL is set.",
        "omni doctor gained a non-blocking check that only validates the graph file's JSON shape when one is present, since building the graph is opt-in.",
        "pyproject.toml exposes tree-sitter + grammars as an optional [graph] extra rather than a hard dependency."
      ],
      "validation_required": [
        "doctor",
        "python_compile",
        "manual_verification"
      ],
      "documentation_required": [
        "changelog",
        "readme"
      ],
      "risk_notes": [
        "Call/inheritance resolution uses deterministic heuristics (same-module lookup, import-alias resolution, unique-name-in-graph fallback) rather than full type inference; ambiguous or dynamically-dispatched references are correctly left EXTRACTED-only rather than guessed, which is intentional but means some real connections will not surface as INFERRED edges in v1.",
        "Nested function/class definitions declared inside another function's body are attributed to the enclosing function rather than modeled as their own node, to keep v1's traversal bounded.",
        "The semantic pass sends a symbol's name, qualified name, and the full list of known symbol qualified_names to the configured API; it is opt-in (--semantic flag plus an explicitly set OMNI_GRAPH_SEMANTIC_API_URL) so nothing leaves the machine by default."
      ]
    },
    {
      "id": "REQ-016",
      "category": "Presentation / brand identity",
      "title": "Restyle OmniEngineering's brand assets to match the ctxrelay/PathBox family",
      "description": "OmniEngineering's presentation assets (assets/banners/omniengineering-*.svg, assets/identity/omniengineering-*.svg) used an unrelated warm-gradient/forged-OE-monogram visual system, while the two sibling projects in the same account (context-relay-mcp's CtxRelay, PathBox) share one visual family: a dark #0f0f12 ground, single #e0475c accent, monospace wordmark, a 48x48 panel-plus-corner-nodes-plus-beacon mark shape, and a matching 820x180 grid/mesh/scanline banner, all under assets/brand/. Replaced OmniEngineering's assets with a new set in that same family -- assets/brand/omni-mark.svg, omni-wordmark.svg, omni-banner.svg -- keeping the shared chrome (panel, corner nodes, beacon, banner grid/mesh/scanline/signature) pixel-identical to the siblings and giving OmniEngineering its own mark content: a central hub fanned out to five small nodes, representing the actual product (one repo-local .ai/ control plane synced out to many AI assistant entrypoints), distinct from CtxRelay's two-endpoint relay and PathBox's chat panel. The wordmark uses the literal `omni` CLI name rather than the full 'OmniEngineering' repo name, matching how the sibling brands use their short product/command name.",
      "priority": "low",
      "status": "completed",
      "minimum_access_scope": [
        "assets/brand/omni-mark.svg",
        "assets/brand/omni-wordmark.svg",
        "assets/brand/omni-banner.svg",
        "README.md",
        "make_ai.py",
        ".gitignore"
      ],
      "acceptance_criteria": [
        "New assets/brand/omni-{mark,wordmark,banner}.svg exist, are well-formed XML, and reuse the exact shared chrome (colors, panel/corner-node/beacon geometry, banner grid/mesh/scanline/signature) from context-relay-mcp's and PathBox's own assets/brand/*.svg, verified by diffing the shared elements against both sibling repos.",
        "The old assets/banners/ and assets/identity/ directories and their omniengineering-*.svg files are removed.",
        "README.md's top banner and its 'optional presentation assets' listing point at assets/brand/ instead of the removed directories.",
        "make_ai.py's ADOPTION_PRESENTATION_FILES points at assets/brand instead of the removed assets/identity and assets/banners paths, so omni adopt --include-presentation still works.",
        ".gitignore no longer excludes assets/brand/ (it silently ignored this exact directory since a June 2026 commit, which would have made the new canonical brand assets untrackable).",
        "omni doctor and omni map both run clean after the change (the architecture diagram at assets/omni-context.svg, which doctor validates by name, is untouched)."
      ],
      "validation_required": [
        "doctor",
        "manual_verification"
      ],
      "documentation_required": [
        "changelog"
      ],
      "risk_notes": [
        "This only restyles presentation/brand assets (logo, banner). The functional architecture diagrams under design/diagrams/ and assets/omni-context.svg were intentionally left in their existing style since they illustrate mechanism, not brand identity, and the request was scoped to 'the look', not every diagram in the repo.",
        "Verified the new SVGs render correctly (matching structure/behavior to the already-shipped sibling files) via a local cairosvg render; a font-fallback rendering artifact on the monospace wordmark text appeared identically on PathBox's own shipped wordmark under the same renderer, confirming it is a local missing-font rendering quirk and not a defect introduced here."
      ]
    },
    {
      "id": "REQ-017",
      "category": "CLI / template maintainability",
      "title": "Fix: adopting or updating omni_graph.py must not be able to break every other command",
      "description": "Found by end-to-end testing REQ-015 against a simulated pre-existing application: (1) ADOPTION_CLI_FILES only listed omni/make_ai.py, so `omni adopt --include-cli` copied a make_ai.py that unconditionally imports omni_graph without ever copying omni_graph.py itself, crashing every single omni command (not just `graph`) in the adopted project with a raw ModuleNotFoundError. (2) Testing the upgrade path (a project adopted before omni_graph.py existed, running `omni update` against a source that now has it) surfaced a deeper, structural gap: `omni update`'s file list (TEMPLATE_MANAGED_FILES) is evaluated by the OLD make_ai.py that is currently running the update, not by the newer --source -- so a target can never discover a brand-new template-managed file on the very first update after it's introduced, no matter what that file list contains by the time of a *later* update. Fixed both: added omni_graph.py to ADOPTION_CLI_FILES (fixes fresh `omni adopt --include-cli` immediately, and fixes `omni update` from the second update onward once a target is running post-fix code), and made make_ai.py's own `import omni_graph` defensive (try/except, falls back to None) with a shared require_omni_graph() guard on every `omni graph ...` subcommand and a doctor warning -- so a target still missing omni_graph.py (the unavoidable one-time gap on that very first update, or any other reason the file might be absent) degrades to 'omni graph is unavailable' instead of taking down doctor/sync/map/every other command.",
      "priority": "high",
      "status": "completed",
      "minimum_access_scope": [
        "make_ai.py"
      ],
      "acceptance_criteria": [
        "omni adopt --target <dir> --include-cli copies omni_graph.py alongside make_ai.py, verified against a scratch pre-existing application directory whose own pre-existing files (README, source, .gitignore) are confirmed byte-for-byte unchanged after adoption.",
        "A project simulated as adopted before omni_graph.py existed (sourced from OmniEngineering@523c47b) that runs `omni update` against a current source no longer crashes: omni doctor, omni sync, and every non-graph command work immediately after that update even though omni_graph.py is still absent on that specific first pass, with a clear doctor warning naming the gap and the fix (re-run omni adopt --include-cli or omni update).",
        "The same project's second `omni update` (now running the fixed make_ai.py) reports 'added: omni_graph.py', after which omni graph build/trace/show all work against the adopted project's own source files.",
        "Adopter-owned files (.ai/project-configuration.md, .ai/project-map.md, .ai/requirements/requirements.json, CHANGELOG.md) verified untouched by both update passes in this test, including a deliberately added custom note/entry in each.",
        "omni doctor and omni graph both continue to work correctly in this repo's own working copy (which always has omni_graph.py) after these changes."
      ],
      "validation_required": [
        "doctor",
        "python_compile",
        "manual_verification"
      ],
      "documentation_required": [
        "changelog"
      ],
      "risk_notes": [
        "The structural update-mechanism gap (a target's own running code decides what files it looks for) is not fully eliminated -- it is closed for every template file added from this point forward (once a target has updated past this fix, it will correctly discover any future new template file on the very next update), but a project that adopted before omni_graph.py existed will still see one warning-but-not-broken update before omni_graph.py actually arrives. A fully general fix would have run_update ask --source's own make_ai.py for its current file manifest via subprocess instead of trusting its own constant; deferred as a larger structural change beyond this fix's scope.",
        "The defensive import is the durable half of this fix: it guarantees a missing omni_graph.py (for this reason or any other -- a bad copy, manual deletion) degrades to one feature being unavailable rather than the whole CLI being unusable, independent of whether the file-list gap above is ever fully closed."
      ]
    },
    {
      "id": "REQ-018",
      "category": "Presentation / documentation",
      "title": "Replace the hierarchical code-map diagram with a real node-link graph",
      "description": "The first design/diagrams/omni-code-map.svg (REQ-015 follow-up) was a hierarchical org-chart-style diagram (boxes, arrows, a text-list panel for doctor checks) built from omni graph build's own output. User feedback: it didn't read as 'a graph' in the conventional sense. Replaced it with design/diagrams/omni-code-graph.svg -- a real force-directed node-link layout (networkx spring_layout) over all 106 real (non-external) symbols and 277 resolved calls/defines edges from the same .ai/project-graph.json, rendered as circles (sized by kind, colored by source module) and lines (calls vs. defines distinguished). No fabricated structure: same underlying data, different -- and correctly graph-shaped -- visual form.",
      "priority": "low",
      "status": "completed",
      "minimum_access_scope": [
        "design/diagrams/omni-code-graph.svg",
        "README.md"
      ],
      "acceptance_criteria": [
        "design/diagrams/omni-code-map.svg (the org-chart version) is removed.",
        "design/diagrams/omni-code-graph.svg exists, is well-formed XML, and renders as a force-directed node-link graph (verified via a local cairosvg render) rather than a hierarchical chart.",
        "README's Design Documents list points at the new file with an accurate description."
      ],
      "validation_required": [
        "doctor",
        "manual_verification"
      ],
      "documentation_required": [
        "changelog"
      ],
      "risk_notes": [
        "Generating this diagram required networkx + numpy for the spring layout; these are one-off diagram-authoring tools, not a runtime dependency of omni_graph.py or the [graph] extra, so nothing was added to pyproject.toml."
      ]
    },
    {
      "id": "REQ-019",
      "category": "Code understanding",
      "title": "Add omni graph render: a permanent, stdlib-only SVG visualization command",
      "description": "REQ-018's node-link graph was a one-off script requiring networkx+numpy, run by hand and copied into the repo. User ask: this should be a permanent feature of omni itself that updates as the user's project updates, not a one-off artifact. Added `omni graph render` (and the underlying omni_graph.render()/_spring_layout()/_render_svg()/_ego_network() functions): renders .ai/project-graph.json as a force-directed node-link SVG using a small pure-Python Fruchterman-Reingold layout -- no numpy/networkx dependency, so render works with just the standard library exactly like trace/show (only build needs the [graph] extra). Nodes are colored by source language (python/javascript/typescript) and sized by kind (module > class > function/method); edges distinguish calls/imports/inherits (colored, per edge style) from defines (faint, de-emphasized) from related_to (dashed, semantic-pass edges). --max-nodes caps large graphs to the highest-degree nodes to keep the O(n^2) layout fast; --focus/--depth renders just one symbol's neighborhood instead of the whole codebase. design/diagrams/omni-code-graph.svg was regenerated using this actual shipped command (replacing the prior one-off script's output) so the committed diagram is proof the feature works, not just a description of it.",
      "priority": "high",
      "status": "completed",
      "minimum_access_scope": [
        "omni_graph.py",
        "make_ai.py",
        "README.md",
        "design/diagrams/omni-code-graph.svg",
        ".gitignore"
      ],
      "acceptance_criteria": [
        "omni graph render writes a well-formed SVG node-link graph from .ai/project-graph.json, verified by parsing the output as XML and rendering it to PNG for visual review.",
        "render works via plain system Python with no tree-sitter or third-party layout library installed, verified by running it without the [graph] extra present.",
        "Layout performance verified empirically: 112 real nodes rendered in ~0.2s, 300 nodes (max-nodes cap, including external references) in ~1.3s against this repo's own graph -- fast enough for normal CLI use.",
        "--focus <symbol> --depth N renders only that symbol's N-hop neighborhood, verified against run_doctor at depth 1 producing a correct, legible ego-network.",
        "--max-nodes correctly truncates to the highest-degree nodes and reports truncated:true, verified by rendering with include_external=True against a cap below the real node count.",
        "The project's own root label in the rendered title resolves to a real folder name instead of a literal '.', verified before and after the fix.",
        "design/diagrams/omni-code-graph.svg in this repo is the literal output of running `omni graph render` against this repo, not a hand-tuned or separately-scripted render.",
        "omni doctor and omni graph build/trace/show continue to pass after these changes; .ai/project-graph.svg (the default output path) is gitignored the same way .ai/project-graph.json is, since it's a heavy, regeneratable, opt-in artifact, not a committed navigation aid."
      ],
      "validation_required": [
        "doctor",
        "python_compile",
        "manual_verification"
      ],
      "documentation_required": [
        "changelog",
        "readme"
      ],
      "risk_notes": [
        "The layout is O(n^2) per iteration (a deliberate simplicity/dependency tradeoff over numpy-vectorized force-directed algorithms), which is why --max-nodes defaults to 300 and truncates by degree rather than rendering arbitrarily large graphs unbounded.",
        "Dense, highly-connected clusters (e.g. this repo's own doctor-validator cluster) still show some label overlap at default settings -- an inherent characteristic of force-directed layouts at this node density, not something --max-nodes or --focus fully eliminates; --focus on a smaller neighborhood is the escape hatch for a specific area."
      ]
    },
    {
      "id": "REQ-020",
      "category": "Education taxonomy and content freshness",
      "title": "Implement complete version-aware education taxonomy",
      "description": "Represent all 13 education categories and every taxonomy subcategory, classify courses by the appropriate freshness model, and store certification exams separately from curricula so current testing versions can be verified and retired safely.",
      "priority": "high",
      "status": "completed",
      "minimum_access_scope": [
        "backend/supabase/migrations/007_education_taxonomy.sql",
        "backend/app/api/taxonomy/route.ts",
        "backend/app/api/courses/route.ts",
        "mobile/src/types/index.ts",
        "docs/education-taxonomy.md",
        "CHANGELOG.md"
      ],
      "acceptance_criteria": [
        "All 13 primary categories and 130 subcategories are seeded idempotently.",
        "Certification-, technology-, and academic-aligned content use distinct freshness metadata.",
        "Non-draft exam records require an official objectives source and vendor-verification timestamp.",
        "Authenticated clients can retrieve the ordered taxonomy.",
        "Existing learner tracks and progress remain intact."
      ],
      "validation_required": [
        "taxonomy_count",
        "typescript_format",
        "migration_review"
      ],
      "documentation_required": [
        "data_model",
        "changelog"
      ],
      "risk_notes": [
        "The migration must be applied before the new taxonomy and course-creation endpoints are deployed.",
        "Vendor exam records are intentionally not labeled active until verified against official sources."
      ]
    },
    {
      "id": "REQ-021",
      "category": "Catalog expansion",
      "title": "Seed the complete AWS certification catalog, aligned to the live exam guides",
      "description": "Add a track for every currently offered AWS certification, with domains, weightings, and objectives taken from the official AWS exam guides rather than from memory, and link each track to the education taxonomy and to a versioned credential exam record so exam version changes and retirements can be tracked.",
      "priority": "high",
      "status": "completed",
      "minimum_access_scope": [
        "backend/supabase/seed/tracks/aws.ts",
        "backend/supabase/seed/data.ts",
        "backend/supabase/seed/seed.ts",
        "CHANGELOG.md"
      ],
      "acceptance_criteria": [
        "Every currently offered AWS certification has a track: CLF-C02, AIF-C01, SAA-C03, DVA-C02, SOA-C03, DEA-C01, MLA-C02, SAP-C02, DOP-C02, SCS-C03, ANS-C01.",
        "Each track's domain weightings match the official exam guide and sum to 100.",
        "Each track records the official objectives URL and the date the guide was last verified against the vendor.",
        "Exams in transition or retirement carry the vendor's dates: DVA-C02 to 2026-12-01, SAP-C02 to 2026-11-17, ANS-C01 to 2026-12-31.",
        "Seeding stays idempotent, including for exam records whose exam_revision is null.",
        "The nine pre-existing tracks seed unchanged."
      ],
      "validation_required": [
        "typescript_format",
        "domain_weight_sum",
        "seed_idempotency"
      ],
      "documentation_required": [
        "changelog"
      ],
      "risk_notes": [
        "Exam guides change without notice. Three of the eleven exams are already in a version transition, so contentReviewDueAt is set on those tracks and lastVendorVerifiedAt must be re-checked before each release.",
        "MLA-C02 is a beta exam; AWS has not published its general-availability date, and the AWS certification page shows a conflicting exam code (ME1-C02) in one table cell.",
        "Objectives are paraphrased from the vendor guides rather than copied, so they need a subject-matter review pass for accuracy, not just currency.",
        "Hands-on lab prompts are not yet authored for these tracks; the labs field is left empty pending the interactive-labs work."
      ]
    },
    {
      "id": "REQ-022",
      "category": "Catalog expansion",
      "title": "Open the nursing strand: Healthcare & Nursing taxonomy, non-exam credentials, and the licensure ladder behind its disclaimer gate",
      "description": "Give nursing a home in the taxonomy as a fourteenth top-level category, widen the credential model so degrees and state licences can be recorded alongside vendor exams, seed the NCLEX-RN course from the NCSBN 2026 test plan, and build the app-side accept screen that lets a learner clear the per-course disclaimer gate the backend already enforces.",
      "priority": "high",
      "status": "in_progress",
      "minimum_access_scope": [
        "backend/supabase/migrations/013_healthcare_nursing_taxonomy.sql",
        "backend/supabase/migrations/014_credential_non_exam_basis.sql",
        "backend/supabase/seed/tracks/nursing.ts",
        "backend/supabase/seed/data.ts",
        "backend/supabase/seed/seed.ts",
        "mobile/src/content/disclaimers.ts",
        "mobile/src/components/DisclaimerGate.tsx",
        "mobile/app/(app)/course/[trackId]/index.tsx",
        "CHANGELOG.md"
      ],
      "acceptance_criteria": [
        "Healthcare & Nursing exists as a top-level category with subcategories for licensure, NCLEX preparation, degrees, specialty and advanced practice, allied health, coding, administration and fundamentals.",
        "credential_exams accepts a row with no exam code, identified instead by the standard it follows, and stays idempotent across re-seeds.",
        "Both NCLEX tracks carry the eight Client Needs subcategories at the NCSBN planning midpoints, summing to 100, with the published range shown to the learner.",
        "The ladder is complete: CNA, LPN/LVN, ADN and BSN alongside the two NCLEX tracks, each gated and each citing the authority it follows.",
        "Seed data can turn the disclaimer gate on, and the NCLEX-RN track does.",
        "A learner opening a gated course sees the disclaimer and can accept it; acceptance is recorded against the account and the course then opens.",
        "The eleven AWS tracks and nine original tracks seed unchanged."
      ],
      "validation_required": [
        "typescript_format",
        "domain_weight_sum",
        "seed_idempotency",
        "manual_verification"
      ],
      "documentation_required": [
        "changelog"
      ],
      "risk_notes": [
        "NCSBN grants reproduction of the test plan to boards of nursing and nonprofit education programs only and prohibits commercial use. The category structure and percentages are followed as fact; all objective phrasing is written fresh rather than copied. A legal read before release is worth having.",
        "Objectives are paraphrased from the test plan's content areas and need a subject-matter review by a licensed nurse, not just a currency check.",
        "The 2026 RN test plan runs to 2029-03-31; the retirement date is recorded so freshness reporting flags it.",
        "New track codes have no per-course emoji or accent colour in the mobile theme, so AWS and nursing courses fall back to a plain icon.",
        "Unit weights on CNA, LPN/LVN, ADN and BSN are our own judgment about study emphasis, not a published blueprint -- no authority apportions those hours. They must never be shown to a learner as exam weightings, unlike the NCLEX percentages which are vendor facts.",
        "AACN revised the Essentials in 2026; the featured concept 'Diversity, Equity, and Inclusion' from the 2021 edition is now 'Access, Connection, and Engagement'. The BSN track carries contentReviewDueAt 2027-09-18 because that document is actively changing."
      ]
    },
    {
      "id": "REQ-023",
      "category": "Mobile authentication",
      "title": "Fix mobile app-close/lock session handling: stop the AppState background listener from over-firing",
      "description": "The login screen, biometric unlock, and the sign-out-on-close behavior in mobile/src/auth/AuthContext.tsx were reported as not working properly. Code review (not a device reproduction, since this environment has no simulator/device) found the AppState 'background' listener that locks or fully logs out a signed-in user was reacting to the raw 'background' event with no debounce. Android has no 'inactive' state, so the account screen's own photo-library/camera picker (mobile/app/(app)/account.tsx, via expo-image-picker) and the Google sign-in browser tab (mobile/src/auth/googleSignIn.ts, via expo-web-browser on both iOS and Android) all report as 'background' too, even though the user never left the app. That immediately locked or logged out a signed-in user mid-task on those flows. Added a short grace period (1500ms) that cancels itself if 'active' returns before it fires, so a transient system-UI interruption is absorbed while a genuine app close or screen lock -- which lasts far longer than the window -- still locks/logs out exactly as before. Login, biometric enrollment/unlock, and the cold-start session-resolution logic were also read closely; no other functional defect was found there.",
      "priority": "high",
      "status": "completed",
      "minimum_access_scope": [
        "mobile/src/auth/AuthContext.tsx",
        "mobile/app/lock.tsx",
        "mobile/app/(auth)/login.tsx",
        "mobile/src/lib/biometric.ts",
        "mobile/src/lib/secureStorage.ts",
        "mobile/app/(app)/account.tsx",
        "mobile/src/auth/googleSignIn.ts",
        "CHANGELOG.md"
      ],
      "acceptance_criteria": [
        "Backgrounding the app for longer than the grace window still locks a biometric-enrolled device or signs out a non-biometric one, unchanged from prior behavior.",
        "Returning to 'active' before the grace window elapses (the shape of a permission dialog, the in-app photo/camera picker, or the Google sign-in browser tab) does not lock or sign out a previously signed-in user.",
        "A hard OS kill during the grace window is still caught on next launch by the existing cold-start session-resolution branch, so no session survives a real close.",
        "mobile typecheck (tsc --noEmit) passes with no new errors."
      ],
      "validation_required": [
        "typescript_format",
        "manual_verification"
      ],
      "documentation_required": [
        "changelog"
      ],
      "risk_notes": [
        "Verified by static code review and `tsc --noEmit` only -- this environment has no iOS/Android simulator or device, so the actual background/foreground timing (photo picker, camera, Google sign-in browser handoff) still needs a manual pass on a real device or simulator before release.",
        "The 1500ms grace window is a judgment call, not a spec'd value -- long enough to absorb a permission-dialog or picker hand-off, short enough that a real screen-lock or app-switch away still locks/signs out promptly. Worth revisiting if real-device testing shows it too short (a slow Android photo-library launch) or unnecessarily long."
      ]
    },
    {
      "id": "REQ-024",
      "category": "Mobile authentication",
      "title": "Fix login-screen autofill: password did not follow a tapped email suggestion",
      "description": "Reported: on the mobile login screen, tapping a suggested email did not also fill in the saved password. The email field carried textContentType=\"username\" (correct) but autoComplete=\"email\" (mismatched) -- \"email\" is a plain-data autofill hint (the same bucket used for shipping/contact forms), not the credential-identifier hint the OS pairs with a saved login's password. A value picked from that suggestion list is therefore just an email address, with no password attached to bring along. register.tsx already paired its equivalent field correctly (textContentType=\"username\" + autoComplete=\"username\"); login.tsx did not match it. Changed login's email field to autoComplete=\"username\" and added importantForAutofill=\"yes\" to both the email and password fields (the explicit Android autofill opt-in some providers key off of). Widened the shared TextField component's prop passthrough (mobile/src/components/ui.tsx) to allow importantForAutofill, which it did not previously forward.",
      "priority": "high",
      "status": "completed",
      "minimum_access_scope": [
        "mobile/app/(auth)/login.tsx",
        "mobile/src/components/ui.tsx",
        "CHANGELOG.md"
      ],
      "acceptance_criteria": [
        "Login's email field uses textContentType=\"username\" and autoComplete=\"username\", matching register.tsx's already-correct pairing for the same semantic field.",
        "Both the email and password fields on the login screen pass importantForAutofill=\"yes\" through to the native TextInput.",
        "TextField's prop type accepts importantForAutofill without a TypeScript error.",
        "mobile typecheck (tsc --noEmit) passes with no new errors."
      ],
      "validation_required": [
        "typescript_format",
        "manual_verification"
      ],
      "documentation_required": [
        "changelog"
      ],
      "risk_notes": [
        "Verified by static code review and `tsc --noEmit` only -- this environment has no iOS/Android device or simulator, so the actual OS autofill/credential-pairing behavior still needs a manual pass on a real device before release. Autofill/credential-pairing heuristics are OS- and provider-specific (Google Password Manager, iOS Keychain, third-party password managers) and not fully under this app's control even with correct hints.",
        "forgot-password.tsx's email field intentionally keeps autoComplete=\"email\" -- it is a plain email field with no paired password on that screen, so the mismatch that caused this bug does not apply there."
      ]
    },
    {
      "id": "REQ-025",
      "category": "AWS certification catalog",
      "title": "Accuracy pass: verify all eleven AWS course objectives against official exam guides",
      "description": "Checked every AWS track in backend/supabase/seed/tracks/aws.ts against its current official AWS exam guide (fetched from docs.aws.amazon.com) domain by domain, and fixed only genuine gaps -- an entire task or skill statement the guide names that had no corresponding bullet anywhere in that unit, not a trimmed example inside an already-covered bullet. Cloud Practitioner (CLF-C02), Solutions Architect Associate (SAA-C03), AI Practitioner (AIF-C01), Developer Associate (DVA-C02), and Advanced Networking Specialty (ANS-C01) needed real additions. CloudOps Engineer Associate (SOA-C03), Data Engineer Associate (DEA-C01), Machine Learning Engineer Associate (MLA-C02), Solutions Architect Professional (SAP-C02), DevOps Engineer Professional (DOP-C02), and Security Specialty (SCS-C03, checked against the current SCS-C03 exam code rather than the retired SCS-C02) required no changes. No unit weights, gates, or credential metadata changed -- objectives bullets only.",
      "priority": "medium",
      "status": "completed",
      "minimum_access_scope": [
        "backend/supabase/seed/tracks/aws.ts",
        "CHANGELOG.md"
      ],
      "acceptance_criteria": [
        "All eleven AWS tracks' objectives have been compared domain by domain against their official AWS exam guides as fetched on 2026-09-18.",
        "Every added objective corresponds to a task or skill statement the official guide names that had no corresponding bullet anywhere in that unit.",
        "No fix removes or alters an existing accurate objective -- additions only.",
        "backend typecheck (tsc --noEmit) passes with no new errors."
      ],
      "validation_required": [
        "typescript_format",
        "manual_verification"
      ],
      "documentation_required": [
        "changelog"
      ],
      "risk_notes": [
        "Security Specialty has two current-ish exam codes -- SCS-C02, retired 2025-12-01, and SCS-C03, current since 2025-12-02 -- with different domain weightings; confirmed the seed models SCS-C03 before comparing, rather than trusting training-data recall of the exam code.",
        "A sub-bullet example list that dropped one item from an official enumerated list (e.g. one example service named among several) was judged not a real gap, consistent with how earlier tracks in this pass were handled."
      ]
    },
    {
      "id": "REQ-026",
      "category": "Mobile reliability",
      "title": "Stop Google sign-in dead-ending on an unmatched route",
      "description": "On Android the Google OAuth callback returns to com.hel1x.ascendra:/oauth2redirect. Expo Router owns deep linking, finds no route by that name, and renders its built-in unmatched screen, so the user lands on a dead end after choosing their account. Intercept the callback before routing, and give every other unmatched route a way out.",
      "priority": "critical",
      "status": "completed",
      "minimum_access_scope": [
        "mobile/app/+native-intent.tsx",
        "mobile/app/+not-found.tsx"
      ],
      "acceptance_criteria": [
        "An OAuth callback deep link no longer renders the unmatched-route screen.",
        "Real routes are unaffected by the intercept.",
        "An unmatched route shows a usable screen with a destination that respects auth status.",
        "The fix is JavaScript only, so it ships over the air without a new native build."
      ],
      "validation_required": [
        "typescript_format",
        "manual_verification"
      ],
      "documentation_required": [
        "changelog"
      ],
      "risk_notes": [
        "Not reproducible on this machine: Google sign-in is hidden on web, and there is no Android SDK here. The path matcher is unit-tested against the URL shapes Android delivers, but the end-to-end flow has not been observed.",
        "If the app is cold-started by the callback the in-flight promise is gone and the user lands on the login screen to tap again -- correct, but not seamless.",
        "+native-intent.tsx is native-only and changes deep-link handling; Fast Refresh does not reliably pick it up, so testing needs a full restart."
      ]
    },
    {
      "id": "REQ-027",
      "category": "Mobile reliability",
      "title": "Restore saved-credential suggestions on the login screen, and associate the app with the website",
      "description": "REQ-024 changed the email field's autofill hint from 'email' to 'username' so a tapped suggestion would bring its password along. The reasoning was right but the association it depends on did not exist, so Android offered nothing at all -- worse than before. Revert the hint for now and publish Digital Asset Links so the saved web credential becomes offerable in the app.",
      "priority": "high",
      "status": "in_progress",
      "minimum_access_scope": [
        "mobile/app/(auth)/login.tsx",
        "backend/public/.well-known/assetlinks.json",
        "backend/public/.well-known/README.md"
      ],
      "acceptance_criteria": [
        "Tapping the email field offers saved suggestions again.",
        "https://www.ascendra-learn.com/.well-known/assetlinks.json serves the JSON over https with no redirect.",
        "The fingerprint listed matches the certificate the installed app is actually signed with.",
        "Once Google has re-crawled, the hint goes back to 'username' and a tapped suggestion carries its password."
      ],
      "validation_required": [
        "typescript_format",
        "manual_verification"
      ],
      "documentation_required": [
        "changelog"
      ],
      "risk_notes": [
        "The reverted hint restores suggestions but not password pairing; that only arrives once the association is live and re-crawled, and Google's crawl timing is not under our control.",
        "The fingerprint is the EAS-managed keystore used by the preview and production profiles, read from the signed APK. Shipping through Play with Play App Signing means Google re-signs the app and that fingerprint must be added alongside, not substituted.",
        "Serving from backend/public assumes Next.js serves dotfolders; verify with curl after the deploy rather than assuming."
      ]
    },
    {
      "id": "REQ-028",
      "category": "Mobile reliability",
      "title": "Surface over-the-air updates in the app instead of applying them an open late",
      "description": "With stock configuration the app downloads an update in the background and applies it on the next cold start, with no signal to the user -- so a fix lands one open late and nobody knows it is waiting. Prompt when an update is ready, and apply silently only when the user has been away long enough that a reload costs them nothing.",
      "priority": "high",
      "status": "completed",
      "minimum_access_scope": [
        "mobile/src/updates/useAppUpdates.ts",
        "mobile/src/components/UpdateBanner.tsx",
        "mobile/app/_layout.tsx",
        "CHANGELOG.md"
      ],
      "acceptance_criteria": [
        "A downloaded, pending update surfaces a prompt the user can act on.",
        "An update fetched by the stock on-launch check also surfaces, not just one this code fetched.",
        "Returning after five minutes away applies a pending update without asking.",
        "A lesson, PBQ or chat route is never silently reloaded, however long the user was away.",
        "Dismissing hides the prompt for the session and it returns on the next foreground.",
        "Nothing runs in development or Expo Go, where there is no update channel.",
        "Ships over the air: JavaScript only, no native change."
      ],
      "validation_required": [
        "typescript_format",
        "manual_verification"
      ],
      "documentation_required": [
        "changelog"
      ],
      "risk_notes": [
        "Not observed on a device: no Android SDK here, and expo-updates is inert in development, so the paths that matter cannot run locally at all.",
        "AuthContext runs its own AppState listener with a 1500ms lock/sign-out grace timer. The update check acts on the foreground transition to stay clear of it, but the two listeners are independent and their ordering is not guaranteed.",
        "The five-minute threshold and the protected-route list are judgment calls, not measured.",
        "Like any update mechanism, this only takes effect from the bundle after the one that introduces it."
      ]
    },
    {
      "id": "REQ-029",
      "category": "Mobile reliability",
      "title": "Re-pair login autofill with its saved password now the app-site association is live",
      "description": "REQ-027 restored autofill suggestions on the login email field by reverting its autoComplete hint from \"username\" back to \"email\", since Android had no way yet to know the app and ascendra-learn.com were the same product and so offered nothing at all. That reversion was explicitly temporary: \"email\" is a plain-data hint that carries no password, so a tapped suggestion still required typing the password by hand. The two blocking conditions REQ-027 named -- assetlinks.json live, and Google's association verifiable -- are now both confirmed directly rather than assumed.",
      "priority": "medium",
      "status": "completed",
      "minimum_access_scope": [
        "mobile/app/(auth)/login.tsx"
      ],
      "acceptance_criteria": [
        "The login email field's autoComplete hint is \"username\", matching its textContentType and register.tsx's existing pairing.",
        "A suggestion tapped from Google Password Manager fills in its saved password as well as the email.",
        "https://digitalassetlinks.googleapis.com/v1/statements:list confirms the delegate_permission/common.get_login_creds relation for ascendra-learn.com and com.hel1x.ascendra before this change ships."
      ],
      "validation_required": [
        "typescript_format",
        "manual_verification"
      ],
      "documentation_required": [
        "changelog"
      ],
      "risk_notes": [
        "Verified live via curl against the production assetlinks.json and Google's own statements:list API in this session, not assumed from REQ-027's original plan -- but Google can still take time to re-crawl and offer suggestions on a given device even once the association is correct.",
        "Ships over the air: JavaScript only, no native change."
      ]
    },
    {
      "id": "REQ-030",
      "category": "Mobile reliability",
      "title": "Persist a plain native login across a full app close",
      "description": "The cold-start effect in AuthContext.tsx only attempted to resume a session via refresh() on web. On native, with biometric unlock off, it unconditionally treated any stored refresh token as stale, revoked it server-side, and signed the user out -- so a plain login never survived closing and reopening the app on Android, regardless of any \"remember me\" expectation. The original intent was to catch a swipe-away or OS kill that skips the AppState background handler's own sign-out, but the effect applied that treatment to every native cold start, not just ones following a real close.",
      "priority": "high",
      "status": "completed",
      "minimum_access_scope": [
        "mobile/src/auth/AuthContext.tsx"
      ],
      "acceptance_criteria": [
        "With biometric unlock off, force-closing and reopening the app on Android resumes the previous session instead of returning to the login screen.",
        "With biometric unlock on, a cold start still stops at the lock screen rather than silently resuming.",
        "Web's existing resume-via-refresh behavior on cold start is unchanged.",
        "The AppState background listener's own intentional-close sign-out (REQ-023) is unchanged."
      ],
      "validation_required": [
        "typescript_format",
        "manual_verification"
      ],
      "documentation_required": [
        "changelog"
      ],
      "risk_notes": [
        "Not observed on a device in this environment; needs a manual Android pass before relying on it.",
        "Ships over the air: JavaScript only, no native change."
      ]
    }
  ]
}
